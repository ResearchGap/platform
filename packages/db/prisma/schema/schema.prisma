generator client {
  provider            = "prisma-client"
  output              = "../generated"
  moduleFormat        = "esm"
  runtime             = "bun"
  importFileExtension = "js"
}

datasource db {
  provider = "postgresql"
}
